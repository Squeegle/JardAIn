# Docker Compose Environment Variables Template
# Copy this file to .env and update the values for docker-compose usage

# Database Configuration
POSTGRES_DB=jardain
POSTGRES_USER=jardain_user
POSTGRES_PASSWORD=generate_a_secure_password_here
POSTGRES_PORT=5432

# Application Configuration
LLM_PROVIDER=ollama
OLLAMA_BASE_URL=http://host.docker.internal:11434
OLLAMA_MODEL=llama3.1

# Optional: OpenAI Configuration
# OPENAI_API_KEY=your_openai_api_key_here
# OPENAI_MODEL=gpt-3.5-turbo

# Security Notes:
# - Replace 'generate_a_secure_password_here' with a strong password (16+ characters)
# - Use a mix of letters, numbers, and special characters
# - Never commit the .env file to version control
# - Keep your passwords secure and rotate them regularly
# - The automated setup script will generate secure passwords automatically 